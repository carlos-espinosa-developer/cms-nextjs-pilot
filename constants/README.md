# Constants

- **mockData.ts**: ejemplos de bloques del page builder (Hero, Text). Solo documentación/referencia; la app no lo usa en runtime.
- **realData.json**: respuesta de ejemplo de la API GraphQL de WordPress. Solo documentación/referencia; la app se conecta directamente a la API (`NEXT_PUBLIC_WORDPRESS_API_URL`).
